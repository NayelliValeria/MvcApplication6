@model MvcApplication6.Models.tecnologia
@{
    ViewBag.Title = "Registrar nueva tecnología";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<script src="@Url.Content("~/Scripts/CreateCandidato.js")"></script>
<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">@ViewBag.Title</div>
            <div class="panel-body">
                <div class="row">
                    <div class="">
                        @using (Html.BeginForm())
                        {
                            @Html.AntiForgeryToken()
                            @Html.ValidationSummary(true, "Campos con valores no validos.")
                             <form role="form">
                                 <div class="col-xs-6 form-group">
                                     @Html.LabelFor(model => model.nombre)
                                     @Html.TextBoxFor(model => model.nombre, new { @class = "form-control", @onblur="javascript:eliminarEspacios(this)" })
                                     @Html.ValidationMessageFor(model => model.nombre)
                                 </div>
                                 <div class="boton2">
                                     <input type="submit" value="Guardar" class="btn btn-default" />
                                 </div>
                             </form>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

